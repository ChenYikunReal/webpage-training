# CSS笔记

## CSS选择器

1.标签选择器
```css
a {
    font-size=10px;
    color=#F93;
}
```

2.类别选择器(`class="one"`)
```css
.one {
    font-family:宋体;
    font-size:30px;
    color:red;
}
```

3.id选择器(`id="first"`)
```css
#first {
    font-size:15px;
}
```

## CSS引入方式
- 行内样式
- 内嵌式
- 链接式(有效)
